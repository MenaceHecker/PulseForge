spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/pulseforge
    username: pulseforge
    password: pulseforge

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
